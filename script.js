// Get the current year
const currentYear = new Date().getFullYear();

// Find the element with the ID 'current-year' and update its content
document.getElementById('current-year').textContent = currentYear;

// You could add more complex JS here later, such as:
// 1. A function to toggle a mobile navigation menu.
// 2. Form validation for a contact form.
// 3. A simple image slider for showcasing projects.